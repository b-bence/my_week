import React, {useState, createContext} from 'react'

export const PositivesAndNegativesContext = createContext();

const PositivesAndNegatives = () =>{

    const [positives, addPositive] = useState[
        'Relaxed week', 'Good teamwork', 'Effective communication'
    ]

    const [negatives, addNegative] = useState[
        "Unproductive Monday", "Git", "Merge conflict"
    ]


}